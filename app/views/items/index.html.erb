<h1>Items#index</h1>
<p>Find me in app/views/items/index.html.erb</p>

<%= link_to 'Packs', packs_path %>

<% @items.each do |item| %>
  <p><%= item.name %> <%= link_to 'Edit', edit_item_path(item) %> | <%= link_to 'Delete', item_path(item), method: :delete %></p>
<% end %>

<%= link_to 'Add item', new_item_path %>
